spring:
  application:
    name: springapp
  cloud:
    kubernetes:
      config:
        sources:         
         # Spring Cloud Kubernetes will lookup a ConfigMap named c3 in namespace n3
         - namespace: test1
           name: springapp